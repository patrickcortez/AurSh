# Auto-detect text files and normalize line endings
* text=auto

# Shell scripts must always use LF (Unix line endings)
*.sh text eol=lf

# Batch files must always use CRLF (Windows line endings)
*.bat text eol=crlf
